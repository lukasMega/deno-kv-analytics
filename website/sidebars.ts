import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

// Explicit, not autogenerated: a deliberate reading order that runs
// what-it-is -> try-it -> deploy-it -> configure-it, with every "why" pushed
// into design.md at the end so the install path stays short.
const sidebars: SidebarsConfig = {
  docsSidebar: [
    'intro',
    'quickstart',
    'deploy',
    'configuration',
    'multiple-projects',
    'dashboard',
    'privacy',
    'design',
  ],
};

export default sidebars;
